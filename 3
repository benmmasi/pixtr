class GroupMemberships < ApplicationController 
  def create
    @group = Group.find(params[:group_id])
    membership = GroupMembership.new(member_id: current_user.id
                                     group_id: @group.id)
    if group.save
      redirect to groups_path
    else
      render :create
    end 
  end
end
